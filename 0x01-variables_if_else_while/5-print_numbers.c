#include <stdio.h>

/**
 * main - prints all strings from 0 to 9
 * Return: prints all strings from 0 to 9
 */
int main(void)
{
	for (a=0; a<=10; a++)
		putchar(a);

		putchar("\n");

		return (0);
}

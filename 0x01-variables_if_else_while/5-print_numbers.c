#include <stdio.h>

/**
 * main - prints all strings from 0 to 9
 * Return: prints all strings from 0 to 9
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
		putchar(a);

		putchar("\n");

		return (0);
}
